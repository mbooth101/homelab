zone "{{ domain }}" IN {
        type master;
        file "{{ db_root }}/{{ domain }}.zone";
        allow-update { none; };
};

zone "{{ reverse_ptr4 }}" IN {
        type master;
        file "{{ db_root }}/{{ reverse_ptr4 }}.zone";
        allow-update { none; };
};

