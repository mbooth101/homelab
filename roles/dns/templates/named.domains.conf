zone "{{ domain }}" IN {
        type master;
        file "{{ data_dir }}/{{ domain }}.zone";
        allow-update { none; };
};

zone "{{ reverse_ptr4 }}" IN {
        type master;
        file "{{ data_dir }}/{{ reverse_ptr4 }}.zone";
        allow-update { none; };
};

