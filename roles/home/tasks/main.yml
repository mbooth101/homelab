- name: Configure Nginx for Homer Dashboard
  ansible.builtin.include_tasks:
    file: ../../proxy/tasks/configure-site.yml

- name: Ensure Homer Web Directory
  ansible.builtin.file:
    path: "{{ www_root }}"
    state: directory
    mode: '0755'

- name: Download Homer Dashboard
  ansible.builtin.get_url:
    url: "https://github.com/bastienwirtz/homer/releases/download/v{{ version }}/homer.zip"
    dest: "/srv/homer-v{{ version }}.zip"
  register: download

- name: Install Homer Dashboard
  ansible.builtin.unarchive:
    src: "/srv/homer-v{{ version }}.zip"
    dest: "{{ www_root }}"
    remote_src: yes
  when: download.changed

- name: Ensure Homer Assets Directory
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: "{{ item.state }}"
    mode: '0755'
  loop:
    - { path: "{{ www_root }}/assets/tools", state: absent }
    - { path: "{{ www_root }}/assets/services", state: directory }

- name: Copy Homer Assets
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ www_root }}/assets/services"
    mode: '0644'
  with_fileglob:
    - "*.png"
  notify:
    - Reload Nginx

- name: Install Homer Configuration
  ansible.builtin.template:
    src: config.yml
    dest: "{{ www_root }}/assets/config.yml"
    mode: '0644'
