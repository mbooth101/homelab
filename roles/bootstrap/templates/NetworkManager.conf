## {{ ansible_managed }}

# This configuration prevents NetworkManager from activating connections
# created by the initramfs generator at early boot time
[device]
keep-configuration=no
allowed-connections=except:origin:nm-initrd-generator
